# BKG Docker Environment Configuration
# Copy this file to .env and adjust values as needed

# Database
POSTGRES_PASSWORD=cave_dev_password

# Rust/Daemon
RUST_LOG=info
CAVE_OTEL_SAMPLING_RATE=1.0
CAVE_ENABLE_NAMESPACES=true
CAVE_ENABLE_CGROUPS=true

# API URLs (for development)
NEXT_PUBLIC_API_URL=http://localhost:8080
